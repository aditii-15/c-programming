#include <stdio.h>

int main(){
    int a = 5;
    int b = 5;
    int c = a + b;
    printf ("the value of a is %d\n and the value of b is %d\n and the sum is %d\n", a, b, c);
    // Modulus operator is used to get the operator
    printf ("The remainder when a is divied by b is %d\n", a%b);
    return 0;
}